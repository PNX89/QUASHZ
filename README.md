# QUASHZ

A vendor sends a dataset. One verdict decides whether anything may be trained on it, and the
first refusal it issues is for a row whose outcome was not yet decidable when the decision would
have been made.

Under construction. The pre-flight is measured and the code arrives commit by commit.
